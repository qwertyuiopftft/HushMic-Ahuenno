[package]
name = "dpdfnet-ladspa"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]   # cdylib for the LADSPA .so; rlib for the descriptor tests

[dependencies]
ladspa = "0.3.4"
hushmic-denoiser = { path = "../hushmic-denoiser", version = "0.6.0" }
